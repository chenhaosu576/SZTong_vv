.worktrees/

# Dependencies — regenerated by `npm install`
**/node_modules/

# Build output — regenerated by `npm run build` and the sync-pages script
**/dist/

# Logs
*.log

# Local SQL dumps (SZTong.sql 等)
/*.sql

# Dev docs / AI tooling metadata — never push to origin.
# These are local-only artifacts for Claude Code / superpowers workflows.
/CLAUDE.md
/AGENTS.md
/design*.md
/task.md
/.claude/
/docs/superpowers/
/评分标准.docx
